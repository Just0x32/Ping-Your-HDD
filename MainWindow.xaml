<Window x:Class="Cyclic_Ping_Your_HDD.MainWindow"
        x:Name="MainWindowClass"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:tb="http://www.hardcodet.net/taskbar"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Cyclic_Ping_Your_HDD"
        mc:Ignorable="d"
        ResizeMode="CanMinimize" WindowStartupLocation="CenterScreen"
        Title="Cyclic Ping Your HDD" MinHeight="135" MaxHeight="135" Height="135" MinWidth="300" MaxWidth="300" Width="300">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="0.7*"/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   VerticalAlignment="Bottom" HorizontalAlignment="Center" Margin="0"
                   Text="Path to ping folder:" />

        <TextBox x:Name="PathTextBox"
                 Grid.Row="1"
                 VerticalAlignment="Stretch" HorizontalAlignment="Stretch" Margin="8 6" />

        <Button x:Name="SavePathButton" Click="SavePathButton_Click"
                Grid.Row="2"
                VerticalAlignment="Stretch" HorizontalAlignment="Stretch" Margin="8 4 8 8"
                Content="Save path" />

        <tb:TaskbarIcon Visibility="Visible" ToolTipText="CPY-HDD" TrayLeftMouseDown="TaskbarIcon_TrayLeftMouseDown" />
    </Grid>
</Window>
